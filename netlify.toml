[build]
publish = "public"
command = "hugo --gc --minify && npx -y pagefind --site public --glob \"post/**/*.html\""

[context.production.environment]
HUGO_VERSION = "0.148.2"
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"
